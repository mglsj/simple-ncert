---
import { Card } from "@/components/markdown";
import { generateRandomId } from "@/lib/helpers";

interface Props {
	id?: string;
	title?: string;
}

const { id = generateRandomId(), title } = Astro.props;
---

<Card id={`definition-${id}`} title={title} heading={5}>
	<slot />
</Card>
